import { DynamicImageHeightDirective } from './dynamic-image-height.directive';

describe('DynamicImageHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicImageHeightDirective();
    expect(directive).toBeTruthy();
  });
});
